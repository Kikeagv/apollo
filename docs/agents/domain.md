# Domain Docs

Before exploring the codebase, read `CONTEXT.md` at the repository root and any relevant ADRs under `docs/adr/`, if they exist. Proceed silently when they do not.

This is a single-context repository:

```
/
├── CONTEXT.md
├── docs/adr/
└── src/
```

Use vocabulary defined in `CONTEXT.md`. Surface any conflict with an existing ADR rather than silently overriding it.
